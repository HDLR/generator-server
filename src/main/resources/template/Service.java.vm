package ${package}.${project}.service;

import ${package}.${project}.service.model.${className};
import com.central.common.model.PageResult;
import ${package}.common.service.ISuperService;

import java.util.Map;

/**
 * ${comments}
 * @author ${author}
 * @date ${datetime}
 */
public interface ${className}Service extends ISuperService<${className}> {
    /**
     * 列表
     * @param params
     * @return
     */
    PageResult<${className}> findList(Map<String, Object> params);
}